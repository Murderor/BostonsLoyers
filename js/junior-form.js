// JavaScript для формы младшего адвоката
document.addEventListener('DOMContentLoaded', function() {
    console.log('Форма младшего адвоката загружена');
    
    // ===== НАСТРОЙКИ DISCORD =====
    const DISCORD_WEBHOOK_URL = 'https://discord.com/api/webhooks/1466132348943990886/uhgl4fKd8meIi5nTIiwT4Ig-JEyHil-vCdakZW5yaaPOBKHQ5n3R4uqjfGK_jrUyWrAl';
    
    // ===== ЛОКАЛЬНАЯ БАЗА ВОПРОСОВ =====
    const questionsDatabase = [
         {
        "id": 1,
        "question": "Какие принципы, закрепленные в Разделе I УК, являются вашими главными союзниками при построении защиты и оспаривании действий обвинения?",
        "category": "УК. Общая часть. Принципы"
    },
    {
        "id": 2,
        "question": "Клиенту вменяют формальный состав преступления. На что вы сделаете акцент в защите, в отличие от дела с материальным составом?",
        "category": "УК. Общая часть. Состав преступления"
    },
    {
        "id": 3,
        "question": "Как принцип гуманизма (ст.1.6 УК) может быть использован при ходатайстве о замене лишения свободы на принудительные работы?",
        "category": "УК. Общая часть. Принципы"
    },
    {
        "id": 4,
        "question": "Ваш клиент совершил преступление, наказание за которое ужесточили новым законом, принятым после деяния. На какой принцип вы ссылаетесь, требуя применения старой, более мягкой санкции?",
        "category": "УК. Общая часть. Действие закона"
    },
    {
        "id": 5,
        "question": "В чем ключевое процессуальное отличие рецидива (ст.2.1 УК) от совокупности преступлений (ст.5.2 УК) при назначении наказания?",
        "category": "УК. Общая часть. Рецидив и совокупность"
    },
    {
        "id": 6,
        "question": "Как вы докажете состояние аффекта у вашего клиента (ст.6.2, 6.4 УК) и к каким последствиям в виде смягчения наказания это приведет?",
        "category": "УК. Преступления против личности"
    },
    {
        "id": 7,
        "question": "Клиента обвиняют в убийстве при превышении пределов необходимой обороны (ст.4, 6.1 УК). Какую линию защиты вы выстроите, опираясь на п.2.1 и 2.2 ст.4 УК?",
        "category": "УК. Обстоятельства, исключающие преступность"
    },
    {
        "id": 8,
        "question": "Вашего клиента задержали сотрудники LSPD по статье с юрисдикцией (F). Какая норма закона является прямым основанием для требования о его немедленном освобождении?",
        "category": "УК. Юрисдикция (ст.2.8)"
    },
    {
        "id": 9,
        "question": "Клиент — «пособник» (ст.3.4 УК). Как вы будете аргументировать его минимальную роль в преступлении, чтобы добиться смягчения наказания или переквалификации?",
        "category": "УК. Соучастие"
    },
    {
        "id": 10,
        "question": "При задержании вашего клиента — государственного служащего — не был вызван прокурор. Какие последствия это влечет и как вы этим воспользуетесь?",
        "category": "ПК. Задержание. Особые случаи"
    },
    {
        "id": 11,
        "question": "Сотрудник произвел первичный обыск до зачтения прав Миранды. Какие доказательства, полученные в результате этого обыска, вы будете оспаривать в первую очередь?",
        "category": "ПК. Задержание. Процедура"
    },
    {
        "id": 12,
        "question": "Вам отказали в предоставлении конфиденциальной беседы с клиентом, сославшись на отсутствие свободной комнаты. Каковы ваши немедленные действия и правовые основания для них?",
        "category": "ПК. Права адвоката"
    },
    {
        "id": 13,
        "question": "Процессуальный час задержания вашего клиента истек, но сотрудник не вынес решение. На какую статью ПК вы ссылаетесь, требуя немедленного освобождения?",
        "category": "ПК. Сроки задержания"
    },
    {
        "id": 14,
        "question": "По вашему требованию был вызван прокурор, но он не прибыл в течение 15 минут. Каков дальнейший алгоритм ваших действий согласно ст.19 ПК?",
        "category": "ПК. Задержание. Роль прокурора"
    },
    {
        "id": 15,
        "question": "Во время допроса следователь задает клиенту наводящие вопросы. Как вы пресечете эти действия, ссылаясь на нормы ПК?",
        "category": "ПК. Допрос. Тактика защиты"
    },
    {
        "id": 16,
        "question": "Вы обнаружили, что доказательство по делу было получено в ходе обыска жилища, проведенного без ордера и не в исключительных случаях. Какое ходатайство вы заявите и к какому результату стремитесь?",
        "category": "ПК. Доказательства. Недопустимость"
    },
    {
        "id": 17,
        "question": "Как вы используете свое право на 5-минутный перерыв во время допроса (ст.51 ПК) для коррекции линии защиты в критический момент?",
        "category": "ПК. Права адвоката"
    },
    {
        "id": 18,
        "question": "Клиенту инкриминируют преступление, где потерпевший — государственный служащий. На какие гарантии независимости и объективности расследования вы будете указывать?",
        "category": "ПК. Принципы"
    },
    {
        "id": 19,
        "question": "В чем состоит практическая разница между Федеральным розыском и Процессуальным розыском с точки зрения оснований для задержания и вашей защиты?",
        "category": "ПК. Розыск"
    },
    {
        "id": 20,
        "question": "Какие три способа фиксации процессуальных нарушений со стороны сотрудников вы используете, чтобы они имели силу в суде?",
        "category": "ПК. Обжалование. Тактика"
    },
    {
        "id": 21,
        "question": "Как вы инициируете процедуру Habeas Corpus для клиента, которого незаконно удерживают на закрытой территории (ЗОТ), и что должно содержаться в HC-ордере?",
        "category": "Закон о Habeas Corpus"
    },
    {
        "id": 22,
        "question": "Суд по HC вынес решение об освобождении вашего клиента, но сотрудники его не исполняют более часа. Какие санкции грозят должностным лицам и в какой орган вы обращаетесь?",
        "category": "Закон о Habeas Corpus"
    },
    {
        "id": 23,
        "question": "Владелец режимного объекта отказывается пускать вас по HC-ордеру. На какую конкретную норму Закона о HC вы ссылаетесь и какую ответственность ему указываете?",
        "category": "Закон о Habeas Corpus"
    },
    {
        "id": 24,
        "question": "Каковы критерии отличия законного задержания от незаконного, которое является прямым основанием для удовлетворения ходатайства о HC?",
        "category": "Закон о Habeas Corpus"
    },
    {
        "id": 25,
        "question": "Как вы будете доказывать в суде по HC, что задержание вашего клиента было произведено с целью воспрепятствования его законной деятельности (митинг, журналистика)?",
        "category": "Закон о Habeas Corpus"
    },
    {
        "id": 26,
        "question": "На какую статью Конституции вы опираетесь, требуя допуска к клиенту с момента фактического задержания, а не с момента оформления протокола?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 27,
        "question": "Как принцип презумпции невиновности (ст.37 Конституции) влияет на ваше требование к обвинению о представлении доказательств на стадии задержания?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 28,
        "question": "Клиента задержали за высказывания в интернете. Как вы будете использовать ст.22 Конституции (свобода мысли и слова) для защиты, разграничивая ее с экстремизмом?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 29,
        "question": "Право на неприкосновенность частной жизни (ст.16 Конституции) нарушено при изъятии личной переписки без ордера. Какие последствия для дела это влечет?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 30,
        "question": "Как право на возмещение вреда (ст.41 Конституции) реализуется в случае, если вашего клиента незаконно продержали в КПЗ 2 часа?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 31,
        "question": "Ваш клиент отказывается свидетельствовать против супруга. На какую конституционную норму вы ссылаетесь, защищая его от обвинения в отказе от дачи показаний?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 32,
        "question": "Судья проявляет явную предвзятость. Какие конституционные принципы (ст.78, 79) и механизмы вы используете для заявления отвода?",
        "category": "Конституция. Судебная власть"
    },
    {
        "id": 33,
        "question": "Как иерархия законов (ст.57 Конституции) помогает вам оспорить незаконный приказ начальника департамента, нарушающий права вашего клиента?",
        "category": "Конституция. Основы строя"
    },
    {
        "id": 34,
        "question": "Клиента — иностранца — хотят депортировать без суда. Какие конституционные гарантии прав человека (гл.II) вы приводите, чтобы остановить это?",
        "category": "Конституция. Права и свободы"
    },
    {
        "id": 35,
        "question": "Как принцип разделения властей (ст.6 Конституции) защищает вашего клиента от давления со стороны исполнительной власти на суд?",
        "category": "Конституция. Основы строя"
    },
    {
        "id": 36,
        "question": "Сотрудник LSPD произвел остановку транспортного средства без видимых оснований. На какие нарушения ДК и ПК вы укажете в жалобе?",
        "category": "ДК. Взаимодействие с LSPD"
    },
    {
        "id": 37,
        "question": "Клиента оштрафовали за нарушение ПДД, но квитанцию ему не вручили, а бросили на сиденье. Как вы оспорите законность такого штрафа?",
        "category": "ДК. Процедура штрафа"
    },
    {
        "id": 38,
        "question": "Автомобиль клиента эвакуировали с места, где остановка не была запрещена разметкой или знаками. Какие ваши действия по возврату авто и компенсации?",
        "category": "ДК. Эвакуация ТС"
    },
    {
        "id": 39,
        "question": "Клиента задержали за отказ выйти из машины по требованию сотрудника, не представившегося и не указавшего причину. Правомерно ли задержание?",
        "category": "ДК и ПК. Основания для требований"
    },
    {
        "id": 40,
        "question": "Как вы используете нарушение сотрудником стадий применения силы (ст.33 ПК) при задержании вашего клиента за неповиновение?",
        "category": "ПК. Применение силы"
    },
    {
        "id": 41,
        "question": "Клиенту вменяют получение взятки (ст.15.4 УК). Он утверждает, что деньги были долгом. Как вы будете выстраивать защиту, опираясь на субъективную сторону?",
        "category": "УК. Должностные преступления"
    },
    {
        "id": 42,
        "question": "Дело возбуждено по факту распространения экстремистских материалов (ст.12.4 УК). Как вы докажете отсутствие умысла и общественной опасности в действиях клиента?",
        "category": "УК. Экстремизм"
    },
    {
        "id": 43,
        "question": "Клиент обвиняется в организации преступного сообщества (ст.12.12 УК). Как вы будете отделять его действия от простой групповой преступности?",
        "category": "УК. Организация преступности"
    },
    {
        "id": 44,
        "question": "В деле о мошенничестве (ст.10.2 УК) потерпевший дал противоречивые показания. Как вы построите перекрестный допрос, чтобы дискредитировать его testimony?",
        "category": "Тактика защиты. Допрос"
    },
    {
        "id": 45,
        "question": "Вещественное доказательство (орудие преступления) было изъято без понятых. Какова процедура оспаривания его допустимости и каков вероятный исход?",
        "category": "ПК. Доказательства. Вещественные"
    },
    {
        "id": 46,
        "question": "Свидетель обвинения меняет показания. Как вы используете это в своей защитительной речи, ссылаясь на принцип оценки доказательств (ст.14 ПК)?",
        "category": "Тактика защиты. Судебная речь"
    },
    {
        "id": 47,
        "question": "Клиент хочет заключить сделку о признании вины (плеа-сделка). На каких условиях ст.5.6 УК это возможно и как вы обеспечите минимальное наказание?",
        "category": "УК. Освобождение от ответственности"
    },
    {
        "id": 48,
        "question": "Сроки давности привлечения к уголовной ответственности (ст.5.7 УК) истекли. Какое ходатайство вы заявите и что должно содержать решение суда?",
        "category": "УК. Сроки давности"
    },
    {
        "id": 49,
        "question": "В деле фигурируют несовершеннолетние свидетели. Какие особые процессуальные гарантии их допроса вы проверите на предмет нарушений?",
        "category": "ПК. Особые категории лиц"
    },
    {
        "id": 50,
        "question": "Как вы будете использовать в защите публичный резонанс по делу, превращая его из угрозы в инструмент давления на прокуратуру в рамках закона?",
        "category": "Профессиональная этика и тактика"
    }
    ];
    
    // ===== УПРАВЛЕНИЕ ФОТОГРАФИЯМИ =====
    const photoFields = {
        idCard: {
            uploadArea: document.getElementById('idCardUpload'),
            preview: document.getElementById('idCardPreview'),
            input: document.getElementById('idCard'),
            field: document.getElementById('idCardField'),
            file: null
        },
        statesRole: {
            uploadArea: document.getElementById('statesUpload'),
            preview: document.getElementById('statesPreview'),
            input: document.getElementById('statesRole'),
            field: document.getElementById('statesField'),
            file: null
        }
    };
    
    let uploadedPhotos = 0;
    const photoCountElement = document.getElementById('photoCount');
    
    // Обновление счетчика фото
    function updatePhotoCount() {
        uploadedPhotos = 0;
        Object.values(photoFields).forEach(field => {
            if (field.file) uploadedPhotos++;
        });
        photoCountElement.textContent = uploadedPhotos;
        
        // Подсветка заполненных полей
        Object.values(photoFields).forEach(field => {
            if (field.file) {
                field.field.classList.add('active');
            } else {
                field.field.classList.remove('active');
            }
        });
    }
    
    // Функция загрузки фото
    function loadPhoto(fieldId, file) {
        const field = photoFields[fieldId];
        if (!field) return;
        
        // Проверка типа файла
        if (!file.type.startsWith('image/')) {
            showNotification('Пожалуйста, загружайте только изображения!', 'error');
            return;
        }
        
        // Проверка размера (5MB)
        if (file.size > 5 * 1024 * 1024) {
            showNotification('Размер файла не должен превышать 5MB!', 'error');
            return;
        }
        
        // Удаляем старое фото, если есть
        if (field.file) {
            field.file = null;
        }
        
        field.file = file;
        
        // Показываем превью
        const reader = new FileReader();
        reader.onload = function(e) {
            field.preview.innerHTML = `<img src="${e.target.result}" alt="Превью">`;
            field.preview.style.display = 'block';
            field.uploadArea.querySelector('.upload-placeholder').style.display = 'none';
            field.uploadArea.querySelector('.upload-text').style.display = 'none';
            field.uploadArea.querySelector('.upload-hint').style.display = 'none';
        };
        reader.readAsDataURL(file);
        
        updatePhotoCount();
        console.log(`Фото загружено в поле ${fieldId}: ${file.name}`);
    }
    
    // Функция очистки фото
    window.clearPhoto = function(fieldId) {
        const field = photoFields[fieldId];
        if (!field) return;
        
        field.file = null;
        field.input.value = '';
        field.preview.innerHTML = '';
        field.preview.style.display = 'none';
        field.uploadArea.querySelector('.upload-placeholder').style.display = 'block';
        field.uploadArea.querySelector('.upload-text').style.display = 'block';
        field.uploadArea.querySelector('.upload-hint').style.display = 'block';
        field.field.classList.remove('active');
        
        updatePhotoCount();
    };
    
    // Очистка всех фото
    window.clearAllPhotos = function() {
        Object.keys(photoFields).forEach(fieldId => {
            clearPhoto(fieldId);
        });
        showNotification('Все фото очищены', 'info');
    };
    
    // Настройка drag & drop для всех полей
    Object.keys(photoFields).forEach(fieldId => {
        const field = photoFields[fieldId];
        
        // Клик по области загрузки
        field.uploadArea.addEventListener('click', (e) => {
            if (e.target.classList.contains('photo-control')) return;
            field.input.click();
        });
        
        // Изменение input файла
        field.input.addEventListener('change', (e) => {
            const file = e.target.files[0];
            if (file) {
                loadPhoto(fieldId, file);
            }
        });
        
        // Drag & drop
        field.uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            field.uploadArea.classList.add('dragover');
        });
        
        field.uploadArea.addEventListener('dragleave', () => {
            field.uploadArea.classList.remove('dragover');
        });
        
        field.uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            field.uploadArea.classList.remove('dragover');
            
            const file = e.dataTransfer.files[0];
            if (file) {
                loadPhoto(fieldId, file);
            }
        });
        
        // Вставка через Ctrl+V
        field.uploadArea.addEventListener('paste', (e) => {
            const items = e.clipboardData.items;
            for (let item of items) {
                if (item.type.startsWith('image/')) {
                    const file = item.getAsFile();
                    if (file) {
                        loadPhoto(fieldId, file);
                        showNotification('Фото вставлено из буфера обмена', 'success');
                        break;
                    }
                }
            }
        });
    });
    
    // ===== ЗАГРУЗКА СЛУЧАЙНЫХ ВОПРОСОВ =====
    function loadExamQuestions() {
        try {
            // Выбираем 10 случайных уникальных вопросов
            const selectedQuestions = [];
            const availableQuestions = [...questionsDatabase];
            
            // Перемешиваем вопросы
            for (let i = availableQuestions.length - 1; i > 0; i--) {
                const j = Math.floor(Math.random() * (i + 1));
                [availableQuestions[i], availableQuestions[j]] = [availableQuestions[j], availableQuestions[i]];
            }
            
            // Берем первые 10
            for (let i = 0; i < Math.min(10, availableQuestions.length); i++) {
                selectedQuestions.push(availableQuestions[i]);
            }
            
            // Генерируем HTML для вопросов
            const examContainer = document.getElementById('examQuestions');
            examContainer.innerHTML = '';
            
            selectedQuestions.forEach((question, index) => {
                const questionElement = document.createElement('div');
                questionElement.className = 'exam-question';
                questionElement.innerHTML = `
                    <div>
                        <span class="question-number">${index + 1}</span>
                        <span class="question-category">${question.category}</span>
                    </div>
                    <div class="question-text">${question.question}</div>
                    <textarea 
                        name="answer_${question.id}" 
                        class="form-control" 
                        rows="4" 
                        placeholder="Введите ваш ответ здесь (минимум 50 символов)..."
                        required
                        minlength="50"
                        data-question-id="${question.id}"
                        oninput="updateAnswerCounter(this)"
                    ></textarea>
                    <div class="answer-counter" style="text-align: right; margin-top: 5px; color: #a0a8c0; font-size: 0.85rem;">
                        Символов: <span id="counter_${question.id}">0</span>/50
                    </div>
                `;
                
                examContainer.appendChild(questionElement);
            });
            
            // Инициализируем счетчики
            updateAllAnswerCounters();
            
            console.log(`Загружено ${selectedQuestions.length} случайных вопросов из базы (всего: ${questionsDatabase.length})`);
            
        } catch (error) {
            console.error('Ошибка загрузки вопросов:', error);
            document.getElementById('examQuestions').innerHTML = `
                <div class="error-message" style="text-align: center; padding: 40px; color: #ff6b6b;">
                    <div style="font-size: 3rem; margin-bottom: 20px;">⚠️</div>
                    <h3>Ошибка загрузки вопросов</h3>
                    <p>Пожалуйста, обновите страницу</p>
                    <button onclick="location.reload()" class="button" style="margin-top: 20px;">
                        Обновить страницу
                    </button>
                </div>
            `;
        }
    }
    
    // Функция обновления счетчика символов
    window.updateAnswerCounter = function(textarea) {
        const questionId = textarea.dataset.questionId;
        const counter = document.getElementById(`counter_${questionId}`);
        const length = textarea.value.length;
        
        if (counter) {
            counter.textContent = length;
            
            if (length < 50) {
                counter.style.color = '#ff6b6b';
                textarea.style.borderColor = '#ff6b6b';
            } else {
                counter.style.color = '#2ecc71';
                textarea.style.borderColor = '';
            }
        }
    };
    
    // Обновление всех счетчиков
    function updateAllAnswerCounters() {
        document.querySelectorAll('textarea[data-question-id]').forEach(textarea => {
            const length = textarea.value.length;
            const questionId = textarea.dataset.questionId;
            const counter = document.getElementById(`counter_${questionId}`);
            
            if (counter) {
                counter.textContent = length;
                if (length < 50) {
                    counter.style.color = '#ff6b6b';
                } else {
                    counter.style.color = '#2ecc71';
                }
            }
        });
    }
    
    // ===== ОТПРАВКА В DISCORD =====
    async function sendToDiscord(formData, photoFiles) {
        try {
            showNotification('Отправка заявки в Discord...', 'info');
            
            // Подготавливаем данные для Discord
            const discordData = {
                username: 'Секретарь Авокатуры',
                avatar_url: 'https://i.pinimg.com/originals/7a/af/81/7aaf811aa403514a33e1d468e7405f9a.png',
                thread_name: `Аккредитация: ${formData.fullName} (Младший адвокат)`,
                embeds: [
                    {
                        title: '📋 Новая заявка на аккредитацию',
                        description: `**Младший адвокат**\n${formData.fullName} подал заявку на аккредитацию\n<@&1321503127987421316>, <@&1321503135302291516>, <@&1371785937180426270>`,
                        color: 0xd4af37, // Золотой цвет
                        fields: [
                            {
                                name: '👤 Заявитель',
                                value: formData.fullName,
                                inline: true
                            },
                            {
                                name: '📞 ID спецсвязи',
                                value: `<@${formData.specialCommId}>`,
                                inline: true
                            },
                            {
                                name: '📋 ID заявки',
                                value: formData.applicationId,
                                inline: true
                            },
                            {
                                name: '🕐 Дата подачи',
                                value: new Date(formData.timestamp).toLocaleString('ru-RU'),
                                inline: true
                            },
                            {
                                name: '📷 Документы',
                                value: photoFiles.idCard ? '✅ Удостоверение\n' : '❌ Удостоверение\n' + 
                                       (photoFiles.statesRole ? '✅ STATES' : '❌ STATES'),
                                inline: true
                            }
                        ],
                        footer: {
                            text: 'Адвокатское бюро Majestic RP | Автоматическая система',
                            icon_url: 'https://cdn.discordapp.com/embed/avatars/0.png'
                        },
                        timestamp: new Date().toISOString()
                    }
                ],
                content: `📢 **Новая заявка!** <@${formData.specialCommId}> подал заявку на аккредитацию младшего адвоката.\n\n**📋 Ответы на вопросы:**`
            };
            
            // Форматируем ответы на вопросы
            let answersText = '';
            Object.keys(formData.answers).forEach((questionId, index) => {
                const question = questionsDatabase.find(q => q.id == questionId);
                const answer = formData.answers[questionId];
                
                if (question && answer) {
                    // Обрезаем длинные ответы
                    const shortAnswer = answer.length > 500 ? answer.substring(0, 500) + '...' : answer;
                    answersText += `\n\n**${index + 1}. ${question.question}**\n${shortAnswer}`;
                }
            });
            
            // Добавляем второе сообщение с ответами на вопросы
            discordData.embeds.push({
                title: '📝 Ответы на экзаменационные вопросы',
                description: answersText.length > 4000 ? answersText.substring(0, 4000) + '...' : answersText,
                color: 0x3498db,
                footer: {
                    text: 'Полные ответы доступны в прикрепленных файлах'
                }
            });
            
            // Подготавливаем FormData для отправки файлов
            const discordFormData = new FormData();
            
            // Добавляем JSON данные
            discordFormData.append('payload_json', JSON.stringify(discordData));
            
            // Добавляем фото, если есть
            let fileIndex = 0;
            if (photoFiles.idCard) {
                discordFormData.append(`files[${fileIndex}]`, photoFiles.idCard, 'удостоверение.jpg');
                fileIndex++;
            }
            if (photoFiles.statesRole) {
                discordFormData.append(`files[${fileIndex}]`, photoFiles.statesRole, 'states_role.jpg');
                fileIndex++;
            }
            
            // Добавляем текстовый файл с полными ответами
            const fullAnswersText = `ПОЛНЫЕ ОТВЕТЫ НА ВОПРОСЫ\nЗаявитель: ${formData.fullName}\nID: ${formData.applicationId}\n\n` +
                                  Object.keys(formData.answers).map((questionId, index) => {
                                      const question = questionsDatabase.find(q => q.id == questionId);
                                      return `ВОПРОС ${index + 1} [${question?.category || 'Без категории'}]:\n${question?.question || 'Неизвестный вопрос'}\n\nОТВЕТ:\n${formData.answers[questionId]}\n\n────────────────────\n`;
                                  }).join('\n');
            
            const answersBlob = new Blob([fullAnswersText], { type: 'text/plain' });
            discordFormData.append(`files[${fileIndex}]`, answersBlob, 'полные_ответы.txt');
            
            // Отправляем запрос к Discord Webhook
            const response = await fetch(DISCORD_WEBHOOK_URL, {
                method: 'POST',
                body: discordFormData
            });
            
            if (!response.ok) {
                throw new Error(`Discord API error: ${response.status}`);
            }
            
            const result = await response.json();
            console.log('Заявка успешно отправлена в Discord:', result);
            
            // Получаем ID созданной ветки
            const threadId = result.id;
            if (threadId) {
                // Отправляем второе сообщение с соглашениями
                const agreementsText = '✅ **Соглашения заявителя:**\n' +
                                    '• Подтверждена достоверность документов\n' +
                                    '• Согласен на проверку данных\n' +
                                    '• Готов оплатить взнос $25,000\n' +
                                    '• Ознакомлен с правилами бюро';
                
                await sendFollowupMessage(threadId, agreementsText);
            }
            
            return result;
            
        } catch (error) {
            console.error('Ошибка отправки в Discord:', error);
            throw error;
        }
    }
    
    // Функция для отправки дополнительного сообщения
    async function sendFollowupMessage(threadId, content) {
        try {
            // Для отправки в существующую ветку нужно использовать thread_id параметр
            const followupData = {
                content: content,
                thread_id: threadId
            };
            
            const response = await fetch(DISCORD_WEBHOOK_URL + '?wait=true', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(followupData)
            });
            
            return await response.json();
        } catch (error) {
            console.error('Ошибка отправки дополнительного сообщения:', error);
        }
    }
    
    // ===== ВАЛИДАЦИЯ ФОРМЫ =====
    const form = document.getElementById('juniorForm');
    const submitButton = document.getElementById('submitButton');
    
    form.addEventListener('submit', async function(e) {
        e.preventDefault();
        
        // Проверяем обязательные поля
        const errors = [];
        
        // Проверка имени
        const fullName = document.getElementById('fullName').value.trim();
        if (!fullName) {
            errors.push('• Введите ваше полное имя');
        }
        
        // Проверка ID спецсвязи
        const specialCommId = document.getElementById('specialCommId').value;
        if (!/^\d{17,20}$/.test(specialCommId)) {
            errors.push('• ID спецсвязи должен содержать 17-20 цифр');
        }
        
        // Проверка фото
        if (!photoFields.idCard.file) {
            errors.push('• Загрузите ксерокопию удостоверения личности');
        }
        
        if (!photoFields.statesRole.file) {
            errors.push('• Загрузите скриншот роли в STATES');
        }
        
        // Проверка ответов на вопросы
        const textareas = form.querySelectorAll('textarea[data-question-id]');
        let invalidAnswers = [];
        
        textareas.forEach((textarea, index) => {
            if (textarea.value.length < 50) {
                invalidAnswers.push(index + 1);
                textarea.style.borderColor = '#ff6b6b';
            }
        });
        
        if (invalidAnswers.length > 0) {
            errors.push(`• Ответы на вопросы №${invalidAnswers.join(', ')} должны содержать минимум 50 символов`);
        }
        
        // Проверка соглашений
        const requiredCheckboxes = ['confidentiality', 'agreement','rules'];
        requiredCheckboxes.forEach(id => {
            if (!document.getElementById(id).checked) {
                errors.push(`• Необходимо согласиться с "${document.querySelector(`label[for="${id}"]`).textContent.trim()}"`);
            }
        });
        
        if (errors.length > 0) {
            showNotification('Пожалуйста, исправьте ошибки в форме', 'error');
            
            // Показываем подробные ошибки
            const errorHtml = `
                <div style="text-align: left; margin-top: 10px;">
                    <strong>Обнаружены ошибки:</strong>
                    <ul style="margin: 10px 0 0 0; padding-left: 20px;">
                        ${errors.map(error => `<li style="margin-bottom: 5px;">${error}</li>`).join('')}
                    </ul>
                </div>
            `;
            
            // Создаем модальное окно с ошибками
            const modal = document.createElement('div');
            modal.style.cssText = `
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0,0,0,0.8);
                display: flex;
                align-items: center;
                justify-content: center;
                z-index: 10000;
            `;
            
            modal.innerHTML = `
                <div style="background-color: var(--color-gray); padding: 30px; border-radius: var(--border-radius); max-width: 500px; width: 90%;">
                    <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 20px;">
                        <div style="font-size: 2rem; color: #ff6b6b;">⚠️</div>
                        <h3 style="margin: 0; color: #ff6b6b;">Ошибка отправки формы</h3>
                    </div>
                    ${errorHtml}
                    <button onclick="this.closest('div[style*=\"position: fixed\"]').remove()" 
                            class="button" 
                            style="margin-top: 20px; width: 100%; background-color: #e74c3c;">
                        Закрыть и исправить
                    </button>
                </div>
            `;
            
            document.body.appendChild(modal);
            return;
        }
        
        // Блокируем кнопку отправки
        submitButton.disabled = true;
        submitButton.innerHTML = '⏳ Отправка в Discord...';
        
        // Собираем данные формы
        const formData = {
            fullName: fullName,
            specialCommId: specialCommId,
            position: 'junior_attorney',
            timestamp: new Date().toISOString(),
            applicationId: `APP-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
            answers: {}
        };
        
        // Добавляем ответы на вопросы
        textareas.forEach((textarea) => {
            const questionId = textarea.name.replace('answer_', '');
            formData.answers[questionId] = textarea.value;
        });
        
        try {
            // Отправляем в Discord
            const discordResult = await sendToDiscord(formData, {
                idCard: photoFields.idCard.file,
                statesRole: photoFields.statesRole.file
            });
            
            // Показываем успешное сообщение
            showSuccessMessage(formData.applicationId, discordResult);
            
            // Сохраняем данные в localStorage
            saveApplicationToStorage(formData, discordResult);
            
            // Очищаем форму
            form.reset();
            clearAllPhotos();
            updateAllAnswerCounters();
            
        } catch (error) {
            console.error('Ошибка отправки:', error);
            showNotification('Ошибка отправки заявки. Пожалуйста, попробуйте еще раз.', 'error');
            
            // Разблокируем кнопку
            submitButton.disabled = false;
            submitButton.innerHTML = '📝 Отправить на аккредитацию';
            return;
        }
        
        // Разблокируем кнопку
        submitButton.disabled = false;
        submitButton.innerHTML = '📝 Отправить на аккредитацию';
    });
    
    // ===== СОХРАНЕНИЕ ЧЕРНОВИКА =====
    document.getElementById('saveDraftButton').addEventListener('click', function() {
        const draftData = {
            fullName: document.getElementById('fullName').value,
            specialCommId: document.getElementById('specialCommId').value,
            timestamp: new Date().toLocaleString(),
            answers: {}
        };
        
        // Сохраняем ответы на вопросы
        document.querySelectorAll('textarea[data-question-id]').forEach(textarea => {
            const questionId = textarea.dataset.questionId;
            draftData.answers[questionId] = textarea.value;
        });
        
        // Сохраняем соглашения
        draftData.confidentiality = document.getElementById('confidentiality').checked;
        draftData.agreement = document.getElementById('agreement').checked;
        //draftData.payment = document.getElementById('payment').checked;
        draftData.rules = document.getElementById('rules').checked;
        
        localStorage.setItem('juniorDraft', JSON.stringify(draftData));
        localStorage.setItem('draftSaved', new Date().toLocaleString());
        
        // Показываем уведомление
        showNotification('Черновик сохранён! Вы можете вернуться к нему позже.', 'success');
        console.log('Черновик сохранён:', draftData);
    });
    
    // ===== ЗАГРУЗКА ЧЕРНОВИКА =====
    function loadDraft() {
        const draft = localStorage.getItem('juniorDraft');
        if (draft) {
            try {
                const draftData = JSON.parse(draft);
                
                // Заполняем поля
                document.getElementById('fullName').value = draftData.fullName || '';
                document.getElementById('specialCommId').value = draftData.specialCommId || '';
                
                // Заполняем ответы
                if (draftData.answers) {
                    Object.keys(draftData.answers).forEach(questionId => {
                        const textarea = document.querySelector(`textarea[data-question-id="${questionId}"]`);
                        if (textarea) {
                            textarea.value = draftData.answers[questionId];
                            updateAnswerCounter(textarea);
                        }
                    });
                }
                
                // Заполняем соглашения
                if (draftData.confidentiality !== undefined) {
                    document.getElementById('confidentiality').checked = draftData.confidentiality;
                }
                if (draftData.agreement !== undefined) {
                    document.getElementById('agreement').checked = draftData.agreement;
                }
                if (draftData.payment !== undefined) {
                    //document.getElementById('payment').checked = draftData.payment;
                }
                if (draftData.rules !== undefined) {
                    document.getElementById('rules').checked = draftData.rules;
                }
                
                console.log('Черновик загружен');
                showNotification('Черновик загружен из сохранения', 'info');
                
            } catch (error) {
                console.error('Ошибка загрузки черновика:', error);
            }
        }
    }
    
    // ===== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ =====
    function showSuccessMessage(applicationId, discordResult) {
        const message = document.createElement('div');
        message.style.cssText = `
            position: fixed;
            top: 20px;
            right: 20px;
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            color: white;
            padding: 20px 30px;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            z-index: 10000;
            animation: slideIn 0.5s ease;
            max-width: 400px;
        `;
        
        let discordInfo = '';
        if (discordResult && discordResult.id) {
            discordInfo = `<p style="margin: 5px 0 0 0; font-size: 0.9em; opacity: 0.8;">
                Создана ветка в Discord: ${discordResult.id}
            </p>`;
        }
        
        message.innerHTML = `
            <div style="display: flex; align-items: center; gap: 15px;">
                <div style="font-size: 2rem;">✅</div>
                <div>
                    <h3 style="margin: 0 0 10px 0;">Заявка отправлена!</h3>
                    <p style="margin: 0; opacity: 0.9;">Номер заявки: ${applicationId}</p>
                    <p style="margin: 5px 0 0 0; font-size: 0.9em; opacity: 0.8;">
                        Ответ поступит в течение 24 часов
                    </p>
                    ${discordInfo}
                </div>
            </div>
        `;
        
        document.body.appendChild(message);
        
        setTimeout(() => {
            message.style.animation = 'slideOut 0.5s ease';
            setTimeout(() => message.remove(), 500);
        }, 5000);
    }
    
    function showNotification(text, type = 'info') {
        const colors = {
            success: '#2ecc71',
            error: '#e74c3c',
            info: '#3498db',
            warning: '#f39c12'
        };
        
        const icon = {
            success: '✅',
            error: '❌',
            info: 'ℹ️',
            warning: '⚠️'
        };
        
        const notification = document.createElement('div');
        notification.style.cssText = `
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: linear-gradient(135deg, ${colors[type]}, ${colors[type]}dd);
            color: white;
            padding: 15px 25px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            z-index: 9999;
            animation: slideIn 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
        `;
        
        notification.innerHTML = `
            <div style="font-size: 1.5rem;">${icon[type]}</div>
            <div>${text}</div>
        `;
        
        document.body.appendChild(notification);
        
        setTimeout(() => {
            notification.style.animation = 'slideOut 0.3s ease';
            setTimeout(() => notification.remove(), 300);
        }, 3000);
    }
    
    function saveApplicationToStorage(formData, discordResult) {
        const applications = JSON.parse(localStorage.getItem('applications') || '[]');
        
        const application = {
            id: formData.applicationId,
            type: 'junior_attorney',
            date: new Date().toLocaleString(),
            status: 'pending',
            fullName: formData.fullName,
            specialCommId: formData.specialCommId,
            discordThreadId: discordResult?.id || null
        };
        
        applications.push(application);
        localStorage.setItem('applications', JSON.stringify(applications));
        localStorage.setItem('lastApplication', JSON.stringify(application));
        
        console.log('Заявка сохранена в хранилище:', application);
    }
    
    // ===== ИНИЦИАЛИЗАЦИЯ =====
    loadExamQuestions();
    updatePhotoCount();
    loadDraft();
    
    // Анимация появления формы
    setTimeout(() => {
        document.querySelectorAll('.form-section').forEach((section, index) => {
            section.style.opacity = '0';
            section.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                section.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                section.style.opacity = '1';
                section.style.transform = 'translateY(0)';
            }, 100 * index);
        });
    }, 500);
    
    console.log('Форма полностью инициализирована');
});

// Добавляем CSS анимации
const style = document.createElement('style');
style.textContent = `
    @keyframes slideIn {
        from {
            transform: translateX(100%);
            opacity: 0;
        }
        to {
            transform: translateX(0);
            opacity: 1;
        }
    }
    
    @keyframes slideOut {
        from {
            transform: translateX(0);
            opacity: 1;
        }
        to {
            transform: translateX(100%);
            opacity: 0;
        }
    }
`;
document.head.appendChild(style);